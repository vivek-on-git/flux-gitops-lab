apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx
  namespace: default
spec:
  interval: 5m

  chart:
    spec:
      chart: .
      sourceRef:
        kind: OCIRepository
        name: nginx
        namespace: flux-system

  values:
    service:
      type: ClusterIP
