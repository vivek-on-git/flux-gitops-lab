apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: aws-load-balancer-controller
  namespace: flux-system
spec:
  interval: 10m

  path: ./clusters/kind/apps/echo-server/aws-load-balancer-controller

  prune: true

  sourceRef:
    kind: GitRepository
    name: flux-system
