apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: echoserver
  namespace: flux-system

spec:
  interval: 1m

  path: ./clusters/kind/apps/echo-server/echoserver

  prune: true
  wait: true

  sourceRef:
    kind: GitRepository
    name: flux-system

#  dependsOn:
 #   - name: shared-alb
