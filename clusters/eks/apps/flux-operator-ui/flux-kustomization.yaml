apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: echoserver
  namespace: flux-system
spec:
  interval: 10m

  path: ./clusters/eks/apps/flux-operator-ui

  prune: true

  sourceRef:
    kind: GitRepository
    name: flux-system
