apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: alb-controller
  namespace: flux-system
spec:
  interval: 10m

  path: ./clusters/eks/apps/echo-server/aws-load-balancer-controller

  prune: true

  sourceRef:
    kind: GitRepository
    name: flux-system
